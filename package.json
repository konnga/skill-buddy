{
  "name": "skillbuddy",
  "version": "0.1.0",
  "private": true,
  "description": "SkillBuddy — visual management platform for AI agent skills (Claude Code, Codex, Cursor, OpenCode, Trae, CodeBuddy, WorkBuddy, ...)",
  "packageManager": "pnpm@10.32.1",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "pnpm --filter @skillbuddy/desktop dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:e2e": "pnpm --filter @skillbuddy/desktop test:e2e",
    "typecheck": "pnpm -r typecheck",
    "dist:mac:arm64": "pnpm --filter @skillbuddy/desktop dist:mac:arm64",
    "dist:win:x64": "pnpm --filter @skillbuddy/desktop dist:win:x64",
    "dist:linux:x64": "pnpm --filter @skillbuddy/desktop dist:linux:x64"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "electron",
      "esbuild",
      "vue-demi"
    ]
  }
}
